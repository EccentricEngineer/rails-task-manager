<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<%= link_to 'Go back to task list', tasks_path %>

<%= link_to 'Edit the task list', edit_task_path(@task) %>

<%= link_to 'Delete the list item', task_path(@task.id), method: :delete %>
